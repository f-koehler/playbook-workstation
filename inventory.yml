all:
  children:
    tailscale:
      hosts:
        mbp2021:
          ansible_host: 127.0.0.1
          ansible_port: 22
          ansible_user: fkoehler
    local:
      hosts:
        localhost:
          ansible_connection: local
